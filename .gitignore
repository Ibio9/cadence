# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Copied out of node_modules by scripts/start.mjs — 34MB of build output.
public/mediapipe/

# Carried over from Cadence when JARVIS replaced it at the root. JARVIS's own
# rules had no line for .env, which is the one that matters most.
.env
.env.local
.next
.vercel

# Local Claude Code state stays out; the shared launch config stays in.
.claude/*
!.claude/launch.json